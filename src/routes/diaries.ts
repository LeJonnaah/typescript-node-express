import express from 'express';

const router = express.Router();

router.get('/', (_req, res) => {
    res.send('TODO');
});

router.post('/', (_req, res) => {
    res.send('TODO');
});

export default router;